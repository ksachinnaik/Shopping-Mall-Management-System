# Show full content of a couple of docs that clearly contain links
from pyspark.sql import functions as F

(xml_df
 .filter(F.col("content").contains(">>doc:"))
 .select("reference", "content")
 .show(5, truncate=False))
